workflows:
  build-android:
    name: Build Android APK
    max_build_duration: 15
    environment:
      android: true
      java: 17
    scripts:
      - name: Build APK
        script: ./gradlew assembleDebug
    artifacts:
      - app/build/outputs//*.apk